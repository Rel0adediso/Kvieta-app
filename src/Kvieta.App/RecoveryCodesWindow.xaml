<Window x:Class="Kvieta.App.RecoveryCodesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kvieta" Width="520" Height="465" MinWidth="430" MinHeight="370"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize" Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="24"><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
        <TextBlock Text="{DynamicResource RecoveryCodesTitle}" FontSize="22" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" Text="{DynamicResource RecoveryCodesDescription}" Foreground="{DynamicResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,8,0,16" />
        <TextBox x:Name="CodesTextBox" Grid.Row="2" IsReadOnly="True" AcceptsReturn="True" FontFamily="Consolas" FontSize="14" Padding="14" VerticalScrollBarVisibility="Auto" />
        <CheckBox x:Name="AcknowledgementBox" Grid.Row="3" Content="{DynamicResource RecoveryCodesAcknowledgement}" Margin="0,12,0,0" Checked="Acknowledgement_Changed" Unchecked="Acknowledgement_Changed" Visibility="Collapsed"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0"><Button Content="{DynamicResource CopyRecoveryCodes}" Style="{StaticResource SecondaryButtonStyle}" Click="Copy_Click" Margin="0,0,8,0" /><Button x:Name="SaveButton" Content="{DynamicResource SaveRecoveryCodes}" Style="{StaticResource SecondaryButtonStyle}" Click="Save_Click" Margin="0,0,8,0" /><Button x:Name="DoneButton" Content="{DynamicResource Done}" Style="{StaticResource PrimaryButtonStyle}" Click="Done_Click" /></StackPanel>
    </Grid>
</Window>
