<Window x:Class="Kvieta.App.SessionSurfaceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:services="clr-namespace:Kvieta.App.Services"
        Title="Kvieta · Oturum" WindowStyle="None" WindowState="Maximized" ResizeMode="NoResize"
        Topmost="True" ShowInTaskbar="True" Loaded="Window_Loaded" Closing="Window_Closing" Closed="Window_Closed"
        StateChanged="Window_StateChanged" Deactivated="Window_Deactivated" PreviewKeyDown="Window_PreviewKeyDown">
    <Grid Background="{DynamicResource BackgroundBrush}">
        <Ellipse Width="640" Height="640" Fill="{DynamicResource PrimarySoftBrush}" Opacity="0.22" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-300,-260,0" IsHitTestVisible="False"/>
        <Ellipse Width="420" Height="420" Fill="{DynamicResource SurfaceRaisedBrush}" Opacity="0.3" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="-180,0,0,-240" IsHitTestVisible="False"/>
        <Grid Margin="42,30">
            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <ContentControl Width="36" Height="36" Template="{StaticResource KvietaAppIconTemplate}" />
                    <StackPanel Margin="11,0,0,0" VerticalAlignment="Center"><TextBlock Text="Kvieta" FontSize="17" FontWeight="SemiBold" /><TextBlock Text="{Binding RotatingThought}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" Margin="0,2,0,0" /></StackPanel>
                </StackPanel>
                <Button x:Name="ExitButton" Content="{DynamicResource ExitPreview}" HorizontalAlignment="Right" Style="{StaticResource SecondaryButtonStyle}" Padding="14,7" Click="ExitPrototype_Click" />
            </Grid>

            <Grid x:Name="SessionSurface" Grid.Row="1" Width="680" HorizontalAlignment="Center" VerticalAlignment="Center">
              <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="28" Padding="56,42" ClipToBounds="True">
               <Grid>
                <TextBlock Text="K" Foreground="{DynamicResource PrimaryBrush}" FontFamily="Segoe UI Variable Display, Segoe UI" FontSize="210" FontWeight="SemiBold" Opacity="0.035" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-62,-10,0" IsHitTestVisible="False"/>
                <StackPanel Width="560" HorizontalAlignment="Center">
                <Border Background="{DynamicResource PrimarySoftBrush}" CornerRadius="6" Padding="12,6" HorizontalAlignment="Center"><TextBlock Text="{Binding StateLabel}" Foreground="{DynamicResource PrimaryBrush}" FontSize="11" FontWeight="SemiBold" /></Border>
                <TextBlock Text="{Binding Headline}" FontSize="32" FontWeight="SemiBold" TextAlignment="Center" TextWrapping="Wrap" Margin="0,22,0,8" />
                <TextBlock Text="{Binding Description}" Foreground="{DynamicResource MutedTextBrush}" FontSize="14" TextAlignment="Center" TextWrapping="Wrap" />
                <TextBlock Text="{Binding StatusExplanationText}" Foreground="{DynamicResource FaintTextBrush}" FontSize="11" TextAlignment="Center" TextWrapping="Wrap" Margin="0,7,0,0" AutomationProperties.LiveSetting="Polite" />
                <TextBox Text="{Binding FocusIntention, UpdateSourceTrigger=PropertyChanged}" MaxLength="80" Width="360" Height="36" Padding="10,5" Margin="0,12,0,0" Visibility="{Binding HasFocusSession, Converter={StaticResource BooleanToVisibilityConverter}}" ToolTip="{DynamicResource FocusIntentionPrivacy}" AutomationProperties.Name="{DynamicResource FocusIntentionLabel}" />
                <Border Background="{DynamicResource PrimarySoftBrush}" CornerRadius="10" Padding="17,14" Margin="0,16,0,0" Visibility="{Binding HasFocusClosure, Converter={StaticResource BooleanToVisibilityConverter}}"><StackPanel><TextBlock Text="{Binding FocusClosureTitle}" FontSize="18" FontWeight="SemiBold" TextAlignment="Center"/><TextBlock Text="{Binding FocusClosureSummary}" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" TextAlignment="Center" Margin="0,5,0,0"/><TextBlock Text="{Binding FocusClosureGoalProgress}" Foreground="{DynamicResource PrimaryBrush}" FontSize="12" FontWeight="SemiBold" TextAlignment="Center" Margin="0,5,0,10"/><WrapPanel HorizontalAlignment="Center"><Button Content="{DynamicResource ContinueFocus}" IsEnabled="{Binding CanContinueAfterFocus}" Style="{StaticResource PrimaryButtonStyle}" Click="ContinueAfterFocus_Click" Margin="3"/><Button Content="{DynamicResource TakeBreak}" Style="{StaticResource SecondaryButtonStyle}" Click="DismissFocusClosure_Click" Margin="3"/></WrapPanel></StackPanel></Border>
                <Border x:Name="GentleWarningPanel" Background="{DynamicResource SurfaceRaisedBrush}" BorderBrush="{DynamicResource WarningBrush}" BorderThickness="1" CornerRadius="10" Padding="16,13" Margin="0,16,0,0" Visibility="Collapsed" AutomationProperties.LiveSetting="Polite">
                    <StackPanel><TextBlock x:Name="GentleWarningTitle" FontSize="15" FontWeight="SemiBold" TextAlignment="Center"/><TextBlock x:Name="GentleWarningDescription" Foreground="{DynamicResource MutedTextBrush}" FontSize="11.5" TextAlignment="Center" Margin="0,4,0,10"/><WrapPanel HorizontalAlignment="Center"><Button Content="{DynamicResource WorkSaved}" Style="{StaticResource SecondaryButtonStyle}" Click="WorkSaved_Click" Margin="3"/><Button Content="{DynamicResource TakeControlledBreak}" Style="{StaticResource SecondaryButtonStyle}" Click="WarningBreak_Click" Margin="3"/><Button Content="{DynamicResource RequestTime}" Style="{StaticResource SecondaryButtonStyle}" Visibility="{Binding CanRequestExtraTime, Converter={StaticResource BooleanToVisibilityConverter}}" Click="WarningExtraTime_Click" Margin="3"/><Button Content="{DynamicResource EditTomorrowPlan}" Style="{StaticResource PrimaryButtonStyle}" Visibility="{Binding CanPlanTomorrow, Converter={StaticResource BooleanToVisibilityConverter}}" Click="WarningTomorrowPlan_Click" Margin="3"/></WrapPanel></StackPanel>
                </Border>
                <Border Background="{DynamicResource WarningSoftBrush}" CornerRadius="6" Padding="12,7" HorizontalAlignment="Center" Margin="0,12,0,0" Visibility="{Binding IsBlocked, Converter={StaticResource BooleanToVisibilityConverter}}"><TextBlock Text="{Binding BlockedReasonText}" Foreground="{DynamicResource WarningBrush}" FontSize="12" FontWeight="SemiBold" TextAlignment="Center" /></Border>
                <TextBlock Text="{DynamicResource OutsideScheduleHint}" Foreground="{DynamicResource WarningBrush}" FontSize="11" TextAlignment="Center" TextWrapping="Wrap" Margin="0,8,0,0" Visibility="{Binding IsRegularOutsideSchedule, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <Button Content="{DynamicResource TrustCurrentClockAction}" Style="{StaticResource SecondaryButtonStyle}"
                        HorizontalAlignment="Center" Margin="0,12,0,0" Padding="14,7"
                        Visibility="{Binding IsClockRollbackDetected, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Click="TrustCurrentClock_Click" />
                <Border Background="{DynamicResource SurfaceRaisedBrush}" CornerRadius="22" Padding="26,22" Margin="0,30,0,0">
                    <StackPanel>
                        <TextBlock Text="{Binding TimeMetricLabel}" Foreground="{DynamicResource FaintTextBrush}" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Center" />
                        <TextBlock Text="{Binding RemainingText}" services:MotionAssist.AnimateTextChanges="True" Foreground="{DynamicResource PrimaryBrush}" FontSize="68" FontWeight="Light" HorizontalAlignment="Center" Margin="0,7,0,0" />
                        <TextBlock Text="{Binding PauseDurationText}" Foreground="{DynamicResource PrimaryBrush}" FontSize="13" HorizontalAlignment="Center" Margin="0,7,0,0" />
                        <ProgressBar services:MotionAssist.AnimatedValue="{Binding UsagePercent, Mode=OneWay}" Maximum="100" Height="5" Margin="0,24,0,9" Visibility="{Binding HasCountdown, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <Grid Visibility="{Binding HasCountdown, Converter={StaticResource BooleanToVisibilityConverter}}"><TextBlock Text="{Binding UsedDisplayText}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" /><TextBlock Text="{Binding LimitDisplayText}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" HorizontalAlignment="Right" /></Grid>
                    </StackPanel>
                </Border>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,32,0,0">
                    <Button Content="{Binding PrimaryActionText}" Style="{StaticResource PrimaryButtonStyle}" Visibility="{Binding CanStartOrResume, Converter={StaticResource BooleanToVisibilityConverter}}" Click="StartOrResume_Click" Margin="0,0,8,0" />
                    <Button Content="{DynamicResource EndSession}" Style="{StaticResource SecondaryButtonStyle}" Visibility="{Binding CanEndSession, Converter={StaticResource BooleanToVisibilityConverter}}" Click="EndSession_Click" Margin="0,0,8,0" />
                    <Button Content="{DynamicResource RequestTime}" Style="{StaticResource SecondaryButtonStyle}" Visibility="{Binding CanRequestExtraTime, Converter={StaticResource BooleanToVisibilityConverter}}" Click="RequestTime_Click" Margin="0,0,8,0" />
                    <Button Style="{StaticResource SecondaryButtonStyle}" Click="PowerMenu_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{DynamicResource DevicePower}" VerticalAlignment="Center" />
                            <Viewbox Width="13" Height="13" Margin="7,0,0,0" VerticalAlignment="Center">
                                <Grid Width="24" Height="24">
                                    <Path Data="M12,2 L12,12" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="2.2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                                    <Path Data="M7.05,4.11 A9,9 0 1 0 16.95,4.11" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="2.2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Fill="Transparent" />
                                </Grid>
                            </Viewbox>
                        </StackPanel>
                    </Button>
                </StackPanel>
                </StackPanel>
               </Grid>
              </Border>
            </Grid>
            <TextBlock Grid.Row="2" Text="{DynamicResource BreakHint}" Foreground="{DynamicResource FaintTextBrush}" FontSize="11" HorizontalAlignment="Center" />
        </Grid>

        <Border x:Name="PowerOverlay" Background="{DynamicResource OverlayBrush}" Visibility="Collapsed">
            <Border x:Name="PowerCard" Style="{StaticResource CardStyle}" Width="420" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="24">
                <StackPanel><TextBlock Text="{DynamicResource DeviceOptions}" FontSize="22" FontWeight="SemiBold" /><TextBlock Text="{DynamicResource PowerSaveHint}" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" Margin="0,6,0,20" />
                    <Button Content="{DynamicResource Sleep}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,0,8" Click="Sleep_Click" /><Button Content="{DynamicResource Restart}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,0,8" Click="Restart_Click" /><Button Content="{DynamicResource Shutdown}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,0,16" Click="Shutdown_Click" /><Button Content="{DynamicResource Cancel}" Style="{StaticResource PrimaryButtonStyle}" Click="ClosePowerMenu_Click" />
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>
